cerrajerialjr.pages.dev {
    encode gzip

    @backend_routes path /_event/* /ping /_upload /_upload/*
    handle @backend_routes {
        reverse_proxy app:8000
    }

    root * /srv
    file_server
}
